# .gitlab-ci/validation.yml

lint:
  stage: lint
  image: rust:1.92.0
  extends: .cargo_cache
  rules:
    - if: $CI_COMMIT_TAG
  script:
    - rustup component add rustfmt clippy
    - cargo fmt --all -- --check
    - cargo clippy -- -D warnings

test:
  stage: test
  image: rust:1.92.0
  extends: .cargo_cache
  rules:
    - if: $CI_COMMIT_TAG
  script:
    - cargo test --all
